#!/bin/bash

#
# Copyright (C) 2025 Nethesis S.r.l.
# SPDX-License-Identifier: GPL-3.0-or-later
#

# piler does get the ownership after upgrade to 1.4.7, we must be sure to chown piler:piler

# wait up to 60 seconds for the systemd service piler to start
# elapsed=0
# while [ $elapsed -lt 60 ]; do
#     if systemctl is-active --user --quiet piler-app; then
#         # upgrade set the ownership to ubuntu....upstream issue
#         podman exec -ti piler-app chown -R piler:piler /etc/piler
#         podman exec -ti piler-app chown -R piler:piler /var/piler/store
#         break
#     fi
#     sleep 1
#     elapsed=$((elapsed + 1))
# done
